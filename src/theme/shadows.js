import {defaultColors} from './color';

export const smallShadow = {
  shadowColor: defaultColors.fullBlack,
  shadowOffset: {
    width: 0,
    height: 2,
  },
  shadowOpacity: 0.25,
  shadowRadius: 3.84,

  elevation: 5,
};

export const mediumShadow = {
  shadowColor: defaultColors.fullBlack,
  shadowOffset: {
    width: 0,
    height: 5,
  },
  shadowOpacity: 0.34,
  shadowRadius: 6.27,

  elevation: 10,
};

export const largeShadow = {
  shadowColor: defaultColors.fullBlack,
  shadowOffset: {
    width: 0,
    height: 9,
  },
  shadowOpacity: 0.48,
  shadowRadius: 11.95,

  elevation: 18,
};
