import {defaultColors} from './color';

const rangeSliderStyles = {
  thumbBorderWidth: 1,
  step: 1,
  labelStyle: 'none',
  lineWidth: 2,
  selectionColor: defaultColors.divider,
  blankColor: defaultColors.divider,
};

export default rangeSliderStyles;
